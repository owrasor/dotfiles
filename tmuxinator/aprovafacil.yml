name: aprovafacil
root: ~/Code/aprovafacil

windows:
  - webserver:
      root: /workspace/aprovafacil-webserver
      layout: main-horizontal
      panes:
        - sleep 1 && tmux resize-pane -Z -t aprovafacil:webserver.1 && nvim
        -  # Empty zsh
        -  # Empty zsh
  - webapp:
      root: /workspace/aprovafacil-webapp
      layout: main-horizontal
      panes:
        - sleep 1 && tmux resize-pane -Z -t aprovafacil:webapp.1 && nvim
        -  # Empty zsh
        -  # Empty zsh
  - serverjs:
      root: /workspace/aprovafacil-serverjs
      layout: main-horizontal
      panes:
        - sleep 1 && tmux resize-pane -Z -t aprovafacil:serverjs.1 && nvim
        -  # Empty zsh
  - infra:
      root: /workspace/aprovafacil-infra-dev
      layout: even-vertical
      panes:
        -  # Empty zsh
        -  # Empty zsh
  - AI:
      root: /workspace
      panes:
        - opencode
